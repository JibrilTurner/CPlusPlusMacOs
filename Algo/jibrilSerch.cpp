#include <iostream>
#include <cstring> 

void jSearch(int arr[], int len, int left,int right){
    int middle = len / 2;
    for (int i = 0; i < len )

    std::cout << middle << std::endl;
}

int main(){ 
        int arr[] = {2,6,5,1,4,4,3,4,9};
        int len = sizeof(arr) / sizeof(arr[0]);
        jSearch(arr,len);
        std::cout << arr ;
    return 0;  
}